export const metadata = {
	title: "Accomodations",
	description: "Find Accomodations",
};

export default function Layout({ children }) {
	return <main className="space-y-40 mb-40">{children}</main>;
}
