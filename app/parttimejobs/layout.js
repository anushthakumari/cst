export const metadata = {
	title: "Part Time Jobs",
	description: "Find Part Time Jobs",
};

export default function Layout({ children }) {
	return <main className="space-y-40 mb-40">{children}</main>;
}
